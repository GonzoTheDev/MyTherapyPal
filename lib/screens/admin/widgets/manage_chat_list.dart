import 'package:flutter/material.dart';

class ManageChatList extends StatelessWidget {
  const ManageChatList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Manage Chat List'),
      ),
    );
  }
}